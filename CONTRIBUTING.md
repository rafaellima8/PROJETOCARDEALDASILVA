# Contribuindo

## Requisitos
- Python 3.11+ e SQLite
- `pip install -r requirements.txt`

## Dev rápido
1) Crie `.venv` e instale deps
2) Rode ingestões em `data/` (fora do Git)
3) Suba Streamlit: `python -m streamlit run dashboards/licitanow/Meny.py`

## Testes
`pytest -q`
